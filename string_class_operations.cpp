#include<iostream>
using namespace std;
int main() {
	string s = "Aniket";
	string s1 = "Mane";
	
/*	// 1.  s.length() or s.size()
	cout<<s.length();
	cout<<s.size(); */
	
	
/*	// 2.  s.capacity()
	cout<<s.capacity(); */
	
	
/*	// 3.  s.resize(50)
	s.resize(15);
	cout<<s.capacity(); */ 
	
	
/*	// 4.  s.max_size()
	cout<<s.max_size(); */
	
	
/*	// 5.  s.clear()
	s.clear();
	cout<<s; */
	
	
/*	// 6. s.empty()
	cout<<s.empty(); */
	
	
/*	// 7.  s.append()
	cout<<s.capacity()<<endl;
	s.append("Mane");
	cout<<s<<endl;
	cout<<s.capacity()<<endl; */
	
	
/*	//  8.  s.insert()
	s.insert(3, "Mane", 2);
	cout<<s; */
	
	
/*	//  9.  s.replace()
	s.replace(3,2,"Mane");
	cout<<s; */
	
	
	//  10.  s.erase() : same as s.clear()
	
	
/*	//  11.  s.push_back('z')
	s.push_back('z');
	cout<<s; */
	
	
/*	//  12.  s.pop()
	s.pop_back();
	cout<<s; */
	
	
/*	//  13.  s.swap(s1)
	s.swap(s1);
	cout<<s<<endl;
	cout<<s1; */
	
	
/*	//  14.  s.find()  or s.first_of_find() : finds from left side
   //        s.rfind() or s.last_of_find()  : finds from right side
   cout<<s.find('i'); */
   
   
 /*  //  15.  s.substr(3,2)
   cout<<s.substr(3,2); */
   
   
/*  //  16.  s.compare()
   cout<<s.compare(s1); */
   
   
/*  //  17.  [] = subscript operator
   cout<<s[2]; */
   
   
/*  //  18.  +
   s = s + s1;
   cout<<s; */
   
   
/*   //  19.  =
   s=s1;
   cout<<s; */
	
	
	
	
	return 0;
}
